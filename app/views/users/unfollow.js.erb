$('.user-<%= @user.id %>').html('<%= escape_javascript(render "shared/follow", user: @user) %> ');

// escape javascript function - escapes all the weird characters that would break the javascript function