<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="fixed">
    <nav class="top-bar" data-topbar>
      <section class="tab-bar-section">
        <a class="navbar-brand" href="/"><img alt="logo"  src="/assets/artshare_logo_web.png" width = "120"></a>
      </section>
      
      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <li class="has-dropdown">
            <% if user_signed_in? %>
            <a href="#"><%= current_user.full_name %></a>
            <ul class="dropdown">
              <li><a href="#"><%= link_to "Edit Profile", edit_user_registration_path %></a></li>
              <li><a href="#"><%= link_to "Show Profile", current_user %></a></li>
              <li><a href="#"><%= link_to "Logout", destroy_user_session_path, method: :delete %></a></li>
            </ul>
            <% else %>
            <ul class="right">
              <li><a href="#"><%= link_to "Sign Up", new_user_registration_path %></a></li>
              <li><a href="#"><%= link_to "Sign In", new_user_session_path %></a></li>
            </ul>
          <% end %>
          </li>
        </ul>
      </section>
    </nav>
  </div>
  <div class= "container">
    <%= yield %>
  </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>